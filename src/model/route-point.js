export const createPoint = ({
  id,
  type,
  destination,
  dateFrom,
  dateTo,
  basePrice,
  isFavorite,
  offers
}) => ({
  id,
  type,
  destination,
  dateFrom,
  dateTo,
  basePrice,
  isFavorite,
  offers
});
